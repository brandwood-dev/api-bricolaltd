export enum DepositJobStatus {
  SCHEDULED = 'scheduled',
  NOTIFICATION_SENT = 'notification_sent',
  CAPTURING = 'capturing',
  SUCCESS = 'success',
  FAILED = 'failed',
  CANCELLED = 'cancelled',
}
